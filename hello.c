#include <stdio.h> // includes information about standard library.

main(){ // main is a special function where execution begins for all programs. In this case main is a function taking no arguments with no specified return value.
    printf("hello, world\n"); // calls the printf function from the stdio library with an argument specifying what to be printed.
    // \n is an escape sequence that adds a new line to your text above. A full list of escape sequences is found in Section 2.3 which starts at page 37.
}